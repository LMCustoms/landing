name: Build & Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-push:
    uses: LMCustoms/.github/.github/workflows/docker-build-push.yml@main
    with:
      image-name: "lmcustoms/landing"
    secrets: inherit
