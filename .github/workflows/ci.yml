name: CI

on:
  pull_request:

jobs:
  ci:
    uses: LMCustoms/.github/.github/workflows/ci-node.yml@main
    with:
      node-version: "20"
      run-lint: true
      run-typecheck: false
      run-test: false
      run-build: true
